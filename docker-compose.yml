version: "3"

services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    command:
      -
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    env_file:
      - .env
